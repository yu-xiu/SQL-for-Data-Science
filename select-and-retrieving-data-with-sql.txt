Module objective:

sql definition
sql differs from other languages
database administrator and data scientist
one-to-one, one-to-many, and many-to-many relationships with databases
use SELECT statment and talk about basic syntax roles

-----------------------------------

More detailed objectives:

1. Use an Entity Relationship diagram, describing the data elements, their relationships, and inter-dependencies and determine if the existent data is sufficient to address a business question.

2. Retrieve one or more columns of data from a table that relates to the research topic.

3. Identify a subset of data needed from a column or set of columns and write a SQL query to limit to those results.

4. Create an analysis environment and use INSERT to put data into a table

5. add effective comments in your queries so that one, you can remember what you're doing, and two, so others can review your work.




------------------------
SQL -> interpreter between you and the database

Relational database management system
SQL server
PostgreSQL
MySQL


----------------------
this class uses SQLite
